#include<stdio.h>
#include "main.h"

/**
 *
 */

void more_numbers(void)
{
	int num;
	int stop;

	while (stop != 11)
	{
		for (num = 0, <= 14, num++)
		{

			if (num > 9)
			{
				_putchar((num / 10) + '0');
			}
			stop++;
			_putchar('\n')
		}
	}
}
