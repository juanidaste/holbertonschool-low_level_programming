#include "stdio.h"
#include <main.h>

/**
 * int_strlen - Entry of the program
 * @len: idjiajdijad
 * */

int _strlen(char *s)
{
	int len;

	len = 0;

	while(s[len != '\n'])
	{
		len++;
	}
	return (len);
}
