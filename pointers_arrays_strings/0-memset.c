#include "main.h"

/**
 * _memset - Entry of the program
 * _memset - iwfiobwfibowfib
 * @s: dadadadd
 * @b: adadadadadas
 * @n: dadasdada
 * Return: S
 **/

char *_memset(char *s, char b, unsigned int n)
{

	unsigned int i;


		for (i = 0; i < n; i++)
		{
			s[i] = b;
		}
	
		return (s);
}
