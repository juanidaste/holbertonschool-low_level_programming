#include <stdio.h>
#include "main.h"

/**
 * is_prime_number - Entry of the program
 * is_prime_number - buii3buu
 * @n: wiufbiwf
 * Return: widfbuiw
 */

int is_prime_number(int n)
{
	int i;

	i = 2;

	if (n < 0)
	{
		return (0);
	}

	if (n % i == 0)
	{
		return (0);
	}
	return (1);
}
