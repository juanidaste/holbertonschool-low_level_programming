#include <stdio.h>
#include "main.h"
/**
 * print_alphabet_x10 - Function that prints the alphabet in lowercase 10 times
 * Return: Void
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
{
	return (1);
}
	else
	return (0);
}
